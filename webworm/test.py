# encoding:utf-8
from utils.webworm.WebProcess import *
from selenium import webdriver

# chrome_options = webdriver.ChromeOptions()  # 代理IP,由快代理提供
# proxy = requests.get("http://1.12.181.18:5010/get/").json().get("proxy")
# print(proxy)
# # 设置代理
# chrome_options.add_argument('--proxy-server=htttps://%s' % proxy)
# # 注意options的参数用之前定义的chrome_options
# chrome = webdriver.Chrome(options=chrome_options)
# # 百度查IP
# chrome.get('http://icanhazip.com/')
# response = myResponseWithSelenium()
# response.get("https://www.ip138.com/")
# print(response.page_source)
